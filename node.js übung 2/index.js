const chalk = require('chalk');
const sumAndLog = require('./alter.js');

// Dreimaliger Aufruf der importierten Funktion mit verschiedenen Argumenten
sumAndLog(5, 10);
sumAndLog(8, 2);
sumAndLog(3, 7);
